### 概念
对于单个向量构建成的矩阵，通过行、列数的确定可，R 会自动拆开向量以升维至矩阵

一下均以一个 1 至 100 的数值向量为例
### matrix()
```r
m <- matrix(x , nrow = 20) #指定行数
m <- matrix(x , ncol = 20) #指定列数
```
### dim()
`dim()` 意为 dimension，可以通过一个两个值的向量直接指定向量转化为矩阵后的行数和列数

```r
dim(x) <- c(20 , 5) #直接将x转换为20 × 5的矩阵
```