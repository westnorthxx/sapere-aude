### 概念
时间序列（time series）顾名思义，就是按**时间顺序**组织的一种数据结构，在生信中使用较少，本文仅简单介绍

### xts 包
使用 xts 包可以处理时间序列数据

生成一串时间，输入起始时间和结束时间，指定时间单位即可
```r
date <- seq(from = as.Date("2023/1/1"), to = as.Date("2025/12/1"), by = "month")
```

使用 `xts()` 函数即可将一个向量转化为时间序列数据，两者长度应当一致
```r
ts_data <- xts(vector, date)
```