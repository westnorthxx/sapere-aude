### factor() 函数
因子的创建常基于字符串向量的创建，对于存储了分组标签的字符串向量，使用 `factor()` 可以直接生成**因子**

```r
a <- c('a' , 'b' , 'c')
fator(a)
```

### 水平、标签与顺序的指定
对于一个因子，有以下重要选项：
- **水平** levels：计算机识别并存储的编码
- **标签** labels：水平对应的字符串，供人类理解

```r
factor(a , labels = c('M' , 'F')) #修改标签
facrot
```

标签与水平一一对应，但**水平与字符并不要求对应**