如果你实在想不起来使用哪个选项了，可以在输入 `-` 后，连按两次 `tab`

```bash
wc -
#连续按两次tab
--bytes            --files0-from=     --lines            --total=           --words
--chars            --help             --max-line-length  --version
#查看提示
```

当然，也可以通过[[获取命令帮助]]的方法查看详细的解释