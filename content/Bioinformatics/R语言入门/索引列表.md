基本的索引和其他数据结构基本一样

### 数值索引
使用数值可以直接索引，注意 `[]` 索引的是数组的名字，返回的是**列表**，`[[]]` 可以直接索引**元素本身**
```r
alist[1] #返回第一个元素，是列表
alist[[1]] #返回第一个元素本身
```
### $索引
如果再创建列表时进行了命名，则可以使用 `$` 针对元素名索引
```r
alist$art
```