### 简介
tibble 是 tidyverse 包中的核心组成部分，简单说，tibble 就是一种**改进**的[[数据框]]。

tibble 有一些优点：
- 更友好的**打印显示**：默认打印前 10 行且标注每列的数据类型
- 更严格的**子集提取**：索引单行或列时，将不再输出向量，而保持 tibble 结构
- 不会自动更改变量名
- 不会把字符串自动转为因子

然而，tibble 也存在缺点：
- 不能使用行名等...

### tibble 操作
- [[创建tibble]]
- [[dplyr]]
