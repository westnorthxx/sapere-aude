索引矩阵和[[索引向量]]很相似，坐标从一个变成了两个
### 数值索引
按 `[行,列]` 的形式填写坐标或行列名即可索引，**留空**代表索引整行或整列

```r
state.x77[ , "Income"]
state.x77["Ohio" , ]
state.x77["Ohio" , "Income"]
```
### 逻辑索引
矩阵的索引同样支持逻辑索引，包括**判断**后生成的逻辑值均值矩阵索引和[[向量化操作]]

```r
state.x77[c(T , F) , ] #取出奇数行
state.x77[state.x77 > 10]
```