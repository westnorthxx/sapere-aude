---
created: 2026-01-11 09:08
tags:
---
### 单元格合并
单元格的合并使用 `unite()`，选项参数逻辑为：
- 指定待合并列 A
- 指定待合并列 B
- 指定合并的新**列名**

```r
table5 |> unite(century, year, col = "year", sep = "")
```

### 单元格拆分

```r
table3 |> separate_wider_delim(col = rate, delim = "/", names = c("cases", "pop"))
```
