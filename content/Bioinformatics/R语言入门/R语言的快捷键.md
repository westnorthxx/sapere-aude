R 语言中有诸多重要快捷键，to name but a few:

most important
- `tab` 补齐：函数、参数、选项、变量
- `alt -` 赋值
- `esc` 终止命令，相当于 Linux 中的 \^C 

less important
- `ctrl L` 清屏
- `方向键：上下` 循环历史，和 Linux 一样
- `ctrl R` 搜索历史
- `ctrl shift M` **管道符**

`alt shift K` 查看快捷键列表