在电脑里很多文件前有 `.`，例如 obsidian 中的 `.obsidian` 文件夹，这些文件是**隐藏文件**，不会被 `ls` 命令识别

linxu 中的隐藏文件大多是系统配置文件等，各文件夹的内容如下

| 文件夹名      | 全称               | 内容  |
| --------- | ---------------- | --- |
| `.bashrc` | bash run command |     |