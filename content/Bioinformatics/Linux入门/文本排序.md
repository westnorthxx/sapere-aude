### `sort` 命令
排序的逻辑是对字符串从 0 位开始比对 ASCII 码的大小，因此默认排序 int 时将有些奇怪

`sort` 命令可以直接接受来自[[管道符]]的输出

#### 数值排序
`-n` 选项可以对数字按数值排序，而不是根据 ASCII 码的大小进行排序

在 `-n` 选项下，字符串优先排在数值前

#### 降序
`-r` 选项可进行倒序排序

#### 忽略大小写
`-f` (folds) 可以忽略大小写

忽略大小写=小写默认为大写

#### 去除重复行
`-u` 选项可以去除重复行，与 `-f` 联用可起到只留下大写内容的功能

``