### 绝对路径
由 `/` 根目录开始，是文件的完整坐标

### 相对路径
由**当前目录**开始，比绝对路径更短
```bash
cd ./packages/
#.代表了当前目录
```

> 为减少因工作目录变化带来的问题，更推荐使用绝对路径

### 注意点
- 操作当前目录中的文件，可以直接写文件名，这相当于**省略了相对路径**，即 `script` = `./script`
- 操作不在当前目录中的文件，则应该写完整文件的路径，相对或绝对