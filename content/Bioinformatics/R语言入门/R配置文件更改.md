### 简介
R 中的一些默认选项，比如**小数位数**，**镜像地址**，**包自动加载**等等都存储在**配置文件中**。如果需要更改配置文件，且保证配置文件在每次使用 R 时都生效，需要进行配置文件的修改。这类似于 Linux 的 `.bashrc` 文件的修改。

### 自定义配置文件
在**工作目录下**创建一个配置文件，并在该文件中进行配置文件的更改是最好的选择。

```r
file.edit(".Rprofile")
```