---
时间: 2026-01-09
---
### 描述性统计
`summary()` 函数可以对二维数据的所有列进行描述性统计。在此处考虑更复杂的情况：计算每列的描述性统计量并追加至对应列的末尾（类似 excel 的使用情况）。

```r
mean <- apply(mtcars, 2, mean)
rbind(mtcars, MEAN = mean)
```

使用了：
- `apply()` 函数，见[[二维数据的计算]]
- `bind()` 系列函数，`rbind()` 为按行**装订**，即把向量直接添加到数据框的末尾。`cbind()` 为按列装订，同理

