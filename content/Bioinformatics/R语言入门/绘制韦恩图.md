[[列表]]在 R 中的重要应用就是绘制韦恩图

### 韦恩图：共有与特有
**韦恩图（Venn Diagram）** 是通过闭合曲线的重叠关系，直观展示多个生物学数据集（如差异表达基因集、物种OTU、SNP位点等）之间**共有成员与特有成员**分布情况的标准化可视化工具

### venn()
使用 `venn()` 包可以绘制韦恩图，注意传入的是**列表**数据！
```r
venn(alist)
venn(alist[1:4]) #可以结合索引确定使用哪些列表中的元素绘图
```